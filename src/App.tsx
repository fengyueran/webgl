import React from 'react';
import { HashRouter } from 'react-router-dom';
import { Root } from './views/root';

const App = () => (
  <HashRouter>
    <Root />
  </HashRouter>
);

export default App;
